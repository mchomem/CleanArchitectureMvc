<h3>Account Details</h3>
<hr />

<div>
    @if (Context.User.Identity.IsAuthenticated)
    {
        <dl class="row">
            <dt class="col-sm-2">
                Name
            </dt>
            <dd class="col-sm-10">
                @Context.User.Identity.Name
            </dd>
        </dl>
    }
</div>
